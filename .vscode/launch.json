{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Server",
            "program": "${workspaceFolder}/node_modules/.bin/homebridge",
            "cwd": "${workspaceFolder}",
            "args": [
                "-D",
                "-P",
                "."
            ]
        }
    ]
}